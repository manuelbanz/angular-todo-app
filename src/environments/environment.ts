// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyCaPgu4Y95x01I9a8ou22O2e43U2mVHZII",
      authDomain: "angular-todo-app-manuel.firebaseapp.com",
      databaseURL: "https://angular-todo-app-manuel.firebaseio.com",
      projectId: "angular-todo-app-manuel",
      storageBucket: "angular-todo-app-manuel.appspot.com",
      messagingSenderId: "1015742931002"
  }

};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
